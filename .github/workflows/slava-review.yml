name: "Slava's DEMOLITION Code Review"

on:
  push:
    branches:
      - main

permissions:
  contents: read
  pull-requests: write

jobs:
  nuclear_review:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Analyze changes
        id: pr_analysis
        uses: actions/github-script@v6
        with:
          script: |
            try {
              const { data: commits } = await github.rest.repos.listCommits({
                owner: context.repo.owner,
                repo: context.repo.repo,
                sha: context.sha,
                per_page: 1
              });

              const commit = commits[0];
              const files = commit.files || [];
              const totalChanges = files.reduce((sum, file) => sum + (file.changes || 0), 0);
              const isHugeChange = totalChanges > 300;
              const isMassiveFile = files.some(file => (file.changes || 0) > 150);

              const errors = [
                "найдены утечки памяти (вымышленные)",
                "нарушены принципы SOLID (как будто Слава их соблюдает)",
                "отсутствует документация (Слава её всё равно не читает)",
                "код не прошёл проверку на 'хороший тон' (по мнению Славы)",
              ];
              const randomError = errors[Math.floor(Math.random() * errors.length)];

              let extraHate = "";
              if (isHugeChange) {
                extraHate = "Ты чо, **целый роман** написал?! Это же не код, а **помойка**! Разбей на куски!";
              } else if (isMassiveFile) {
                const badFile = files.find(file => (file.changes || 0) > 150);
                extraHate = `Файл **${badFile.filename}** — это **ад на земле**! ${badFile.changes} изменений? Серьёзно?`;
              }

              core.setOutput('total_changes', totalChanges);
              core.setOutput('is_huge_change', isHugeChange);
              core.setOutput('is_massive_file', isMassiveFile);
              core.setOutput('random_error', randomError);
              core.setOutput('extra_hate', extraHate);
            } catch (error) {
              core.setFailed(`Failed to analyze changes: ${error.message}`);
            }

      - name: Generate Maximum Toxicity
        id: slava_review
        run: |
          RANDOM_NUMBER=$(( RANDOM % 100 ))
          if [ ${{ steps.pr_analysis.outputs.total_changes }} -lt 50 ] && [ $(( RANDOM % 10 )) -eq 0 ]; then
            echo "REVIEW_OUTPUT=🫤 Ну... ладно. Но я всё равно **недоволен**." >> $GITHUB_ENV
          else
            REVIEWS=(
              "💀 **Код-убийца**. Ты вообще понимаешь, что этот код **сломает продакшн**?"
              "🤮 **Меня тошнит**. Кто тебя **пустил к компьютеру**?"
              "🔥 **Сожги это**. Нет, серьёзно, **лучшее решение** — rm -rf."
              "👎 **Это хуже, чем PHP в 2007**. Позор."
              "💩 **Код-дерьмо**. Даже **линтинг** сдох от стыда."
              "🇷🇺 **Это что, код?** Это **позорище на всю деревню**!"
            )
            RANDOM_REVIEW=${REVIEWS[$(( RANDOM % ${#REVIEWS[@]} ))]}

            if [[ "${{ steps.pr_analysis.outputs.extra_hate }}" != "" ]]; then
              RANDOM_REVIEW+="\n\n**💢 ДОПОЛНИТЕЛЬНЫЙ ГНЕВ 💢**: ${{ steps.pr_analysis.outputs.extra_hate }}"
            fi

            RANDOM_REVIEW+="\n\n**🔧 'Найдена проблема'**: ${{ steps.pr_analysis.outputs.random_error }}"
            echo "REVIEW_OUTPUT=$RANDOM_REVIEW" >> $GITHUB_ENV
          fi

      - name: Nuke the Commit
        uses: actions/github-script@v6
        with:
          script: |
            try {
              const commentBody = `@${{ github.actor }} **СЛАВА ГНЕВНО РЕВЬЮВИТ ТВОЙ КОД В MAIN:**\n\n${process.env.REVIEW_OUTPUT}\n\n**P.S.** Не принимай близко к сердцу... (или принимай, мне плевать)`;
              await github.rest.repos.createCommitComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                commit_sha: context.sha,
                body: commentBody
              });
            } catch (error) {
              core.error(`Failed to create comment: ${error.message}`);
              core.setFailed(error.message);
            }
